{
  "name": "paletas-de-whatsapp",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:api": "vercel dev --listen 3000",
    "build": "node scripts/generate-icons.mjs && node scripts/generate-auth-pages.mjs && vite build",
    "test:tts": "node scripts/test-tts.mjs",
    "sync:paletas": "node scripts/sync-paletas-deliverables.mjs",
    "icons": "node scripts/generate-icons.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "firebase": "^11.6.0",
    "firebase-admin": "^13.2.0",
    "resend": "^4.5.1"
  },
  "devDependencies": {
    "sharp": "^0.34.1",
    "vite": "^5.4.0"
  }
}
